---
- apt: update_cache=yes
  become: true

- apt: name={{ item }}
  with_items:
    - python-pip
    - rsync
    - ntp
    - git
    - php5-cli
    - php5-ssh2
  become: true

- pip: name=ansible
  become: true

- synchronize:
    src: "{{ playbook_dir }}"
    dest: "{{ ansible_env['HOME'] }}"
    delete: true
    rsync_opts:
      - "--exclude=vendor_roles"
